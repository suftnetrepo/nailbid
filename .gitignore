
# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli

# ─── Dependencies ─────────────────────────────────────────────────────────────
node_modules/

# ─── Expo ─────────────────────────────────────────────────────────────────────
.expo/
dist/
web-build/

# ─── Native builds (regenerated by `expo prebuild` / `pod install`) ──────────
# ios/.gitignore already covers ios/build, ios/Pods, DerivedData etc. for the
# native project itself — this just covers the top level / android's turn.
android/build/
android/app/build/
android/.gradle/

# ─── Env / secrets ────────────────────────────────────────────────────────────
.env
.env.local
*.p8
*.p12
*.mobileprovision
*.jks
*.keystore

# Android release signing keystore + passwords — never commit. Back this
# up (see credentials/android/README.md) outside of git before first upload.
credentials/

# Build outputs
*.aab
*.apk

# ─── OS / editor cruft ────────────────────────────────────────────────────────
.DS_Store
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
